# Write a Python program to create a histogram from a given list of integers.

def histogram(values):
    for n in values:
        times = n
        output = ""
        while times > 0:
            output += "*"
            times -= 1
        print(output)

histogram([1,2,3,4])
