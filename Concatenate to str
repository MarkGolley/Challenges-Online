# Write a Python program to concatenate all elements in a list into a string and return it


def conc(list):
    result = ""
    for items in list:
        result += items
    return result


print(conc(["Apple", "Pear", "Peach"]))
