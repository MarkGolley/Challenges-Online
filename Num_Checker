# Write a Python program to check whether a specified value is contained in a group of values

def num_checker(num, group):
    for n in group:
        if num == n:
            return True
    else:
        return False

print(num_checker(1, [1,5,8,3]))
print(num_checker(-1, [5,8,3]))
