# Write a Python program that will accept the base and height of a triangle and compute the area.

b = int(input("What is the base input?"))
h = int(input("What is the height input?"))

def t_area(b, h):
    a = h * b / 2
    return a

print(t_area(b, h))
